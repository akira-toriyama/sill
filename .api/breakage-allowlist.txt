# sill — api-guard breakage allow-list.
#
# Each line is a REGEX matched against a `swift package
# diagnose-api-breaking-changes` breakage description. An entry here means "this
# removal is not a break for consumers" — it does NOT mean "we decided to ship a
# break quietly". If a consumer could notice, rate the commit :boom: instead.
#
# rationale: (required marker — api-guard.sh refuses to run without it, so an
# allow-list can never silently become a rubber stamp.) Every entry below must
# carry its own `# why:` line directly above it.

# why: ListPointerAffordance is `package`-scoped — no consumer outside the sill
# package can name it, so renaming its case is invisible beyond this repo. The
# digester reports package symbols alongside public ones; the rename (t-1y9q,
# .grab → .grabbing when the closed hand became a held drag state) is a
# consumer-invisible internal decision-type change.
enumelement ListPointerAffordance\.grab has been renamed to enumelement grabbing

