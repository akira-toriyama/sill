# sill やることリスト

- 現行 **v1.24.0**（**#17a Canvas/エフェクト橋完了 = prism のエフェクト描画6種を公開 `ThemeKitUI` 部品に昇格（`ParticleBurstView`/`InkSplatterView`/`PixelSpriteView`/`LinePetsView`/`PathPetView`/`ChompCorridorView`＋共有 `EffectClock`）・NSViewRepresentable バイト等価昇格（粒子+splatter は Canvas 再実装でなくグロー fidelity 優先・ユーザー裁定）・build-best 汎用 API（path/emitter/sprite/icon 入力）・prism を consumer 化（catalog ladder のみローカル）・ThemeKitUI に Motion+PixelArt 依存追加・PR #71 + `v1.24.0`・2026-06-23／**#16 SwiftUI 提供層（A）完了 = `ThemeKitUI`（prism の14ウィジェット橋を公開 SwiftUI モジュールに昇格 + prism を最初の consumer に・Menu/ToolBar reshape・Field→TextField 改名）・PR #70 + `v1.23.0`・2026-06-23／**#15 ヘッドレス純粋核 + seam + a11y 完了 = `ListCore` 純粋核抽出（byte-identical 委譲）+ controlled/uncontrolled seam（firing setters）+ a11y P0/P1・PR #69 + `v1.22.0`・2026-06-23／**#14b ThemedControl 基底完了 = `ThemedControl: NSControl` に Button/FAB/Checkbox/Chip の相互作用機構を集約 + focus-ring outset トークン化・PR #68 + `v1.21.0`・2026-06-23／#14a DRY 集約完了 = PR #67 + `v1.20.0`・2026-06-22／**#13 トークン基盤完了 = pure `Space`/`Radius`/`Elevation` スケール + `PaletteKit.shadow(_:)` 解決表 + ThemeKit→`Motion` 配線・PR #65 + `v1.19.0`・2026-06-22／**#8 文字 MUI 照合完了 = MUI-grounded 内部 TypeScale + font-fidelity 修正・PR #63 + `v1.18.0`・2026-06-22／**#12 chomp「ChompCorridor」完了 = PR #62 + `v1.17.0`（Ph1–Ph5 一括 squash・ユーザー本採用 2026-06-22）**／#9d 完了 = `Gesture`（wand 由来の純粋 4-way 認識アトム）・PR #60 + `v1.16.0` tagged 2026-06-21／#9c = `TrailGeometry`・PR #59 + `v1.15.0`／#9b = `SplatterShape`・PR #58 + `v1.14.0`／#9a = `ParticleBurst.radiusSpeed`・PR #57 + `v1.13.0`／#7 = `ParticleBurst`・PR #56 + `v1.12.0`／#6 = `Motion`・PR #55／#5 = perch PR #133／#4 = perch PR #132／#3 = PR #54）。**番号が小さいほど先**にやる（ただし **#8〜11 は順番入れ替え可**＝ユーザー裁定 2026-06-20）。**#9 完了（wand effect 一式を sill へ・スコープ=full／連作 9a→9b→9c→9d ✅。9e=wand 本体移植は不要＝ユーザー裁定 2026-06-21＝sill 側 #9 は 9d で完結）。#11+#10 = ✅ 完了（PR #61・prism のみ＝タグ無し・2026-06-21）→ **#12 完了（PR #62 + `v1.17.0`・2026-06-22）→ #8 完了（PR #63 + `v1.18.0`・2026-06-22）= 全 #1–#12 完了。**
- すべて sill 本体の作業（追加は additive・default-off）。
- このファイルが残作業・進捗の**唯一の記録**（git 管理）。
- **🆕 次の大型テーマ #13〜（sill 汎用化 + SwiftUI 化 + 各アプリ適用）= 着手（#13 ✅ 完了 = PR #65 + `v1.19.0`／#14a ✅ 完了 = PR #67 + `v1.20.0`・2026-06-22／#14b ✅ 完了 = PR #68 + `v1.21.0`・2026-06-23／#15 ✅ 完了 = PR #69 + `v1.22.0`・2026-06-23／#16 ✅ 完了 = PR #70 + `v1.23.0`・2026-06-23／#16.5 で 0ベース再接地済＝旧 Phase 2/3 を破棄し新 Phase A/B に再作成／**#17a ✅ 完了 = PR #71 + `v1.24.0`・2026-06-23＝Phase A 最優先 Canvas/エフェクト橋6種を公開 ThemeKitUI へ昇格**）**。0ベース全17パーツ調査の根拠と設計は [`docs/superpowers/specs/2026-06-23-app-by-app-swiftui-deep-dive.md`](superpowers/specs/2026-06-23-app-by-app-swiftui-deep-dive.md)。下の「## 次の大型テーマ」節を参照。

## 次の大型テーマ — #13〜（sill 汎用化 + SwiftUI 化 + 各アプリ適用）

> **方向 確定（2026-06-23 0ベース再接地）。** Phase 1（#13–#15）+ #16（`ThemeKitUI`）完了。番号小さいほど先・各フェーズはセッション分割可。**未達成は各行で明示（暗黙にしない）。**
>
> **🔁 #16.5 で旧 Phase 2/3 を破棄・再作成（2026-06-23）。** 旧計画の前提「各アプリは SwiftUI 化 不可／ThemeKit 利用ゼロ＝pure 収束のみ」は **0ベース再調査（34-agent・全17パーツ code-level）で覆った**。結論＝**全アプリの UI 層は SwiftUI/sill で組める・真の UI hard wall は実質ゼロ**（ネイティブは UI でなく裏方＝画像/入力/窓位置/データに限局）。focus/IME も VM で実証（`.nonactivatingPanel` + ThemeKitUI `TextField` は前面アプリ/メニューバーを奪わず key+IME 可）。**全17パーツの設計・裏方・要追加部品 ＝ [`docs/superpowers/specs/2026-06-23-app-by-app-swiftui-deep-dive.md`](superpowers/specs/2026-06-23-app-by-app-swiftui-deep-dive.md)。**（旧再接地メモ [`2026-06-23-roadmap-regrounding-phase2-3.md`](superpowers/specs/2026-06-23-roadmap-regrounding-phase2-3.md) の「不可」結論は本書で上書き。）
>
> **新計画 ＝ Phase A（sill の SwiftUI 部品を建てる）→ Phase B（各アプリ UI を 0ベースで SwiftUI/sill 再構築）。** apps が sill 初の本格 consumer になり sill の SwiftUI 層（`ThemeKitUI`）を牽引する＝「sill が主役・統一感」の実体化。
>
> **前提（ユーザー 2026-06-22/23）**: 個人開発・**0ベース／破壊的変更／長時間リファクタ 全て OK**・最高品質・プラン再作成 OK・可能な限り SwiftUI（`ThemeKitUI`）へ寄せる。

### Phase 1 — sill ブラッシュアップ（全部 sill 内・additive/リファクタ・B の前工事）
- **#13 トークン基盤 — ✅ 完了（PR #65 + `v1.19.0`・2026-06-22／スコープ=ユーザー裁定「A 推奨バランス」）。** pure `Palette` に `Space`(2/4/6/8/12/16)・`Radius`(2/4/6/8/12) を static-let ramp（`ThemedTransition.Duration` 流儀・Tailwind/MUI grounding）＋ `ElevationToken`/`Elevation`(flat/dp2…dp12＝Button/FAB/Group の実 shadow 値) を TypeToken/TypeRole 流儀＋ `ScaleStep`（全 Sendable・Double・config 非依存）。`PaletteKit` に `ResolvedPalette.shadow(_:)→(opacity,radius,offsetY)` 解決表（`uiFont(_:)` 同型・dy を y-up 用に符号反転）。**配線**: ThemeKit に `Motion` 依存追加（acyclic）・9 widget の `setAnimationDuration(0.16)`→`Duration.enter`・combo/tooltip `PopupFade` を `exit`/`enter` へ（**値不変=feel 不変**）。menu 0.14 はトークン無しで据置・easing は system `.easeOut` 据置（`Easing.standard` 化と shared `layerTransaction` は #14a）。14 ファイルの散乱 radius/gap をトークン化（**値完全保持**・例外は承認済2スナップ keycap5→4／drop-onto5→6 のみ・coupled radii は同値維持）。prism `.palette` に `TokenSpecimen`（3スケール live＋Elevation 行が `shadow(_:)` を駆動）。pure `TokenScaleTests`＋`ElevationResolveTests`（CI のみ）。**繰延（spec 通り #14a）**: Elevation widget 適用（inline struct 統合）・shared `layerTransaction`・`bestContrast` public 化。control-size/密度 Metrics・1〜3px hairline・off-grid(3/5/10/72) は温存（対象外）。検証=`swift build` 緑＋敵対的レビュー（4観点×独立検証・12 agent＝8件→確定 nit 4件修正→0）＋CI 緑。**ライブ目視（2スナップ＋TokenSpecimen）はユーザー委譲**（agent 画面収録不可・[[prism-bench]]＝`.palette` タブ）。**halo タッチアップは随時＝未着手**（#13 トークン消費先）。
- **#14 DRY 集約 + 共有基盤** — #14a（トークン非依存・即着手可: 共有 `layerTransaction`・`bestContrast` public 化・`themeBackingScale`・`CornerPath`・`ControlRole`/`palette.color(for:)`・`Elevation` struct 統合）→ #14b（リテラル→#13 トークン配線）。本丸 `ThemedControl` 基底（hover/press/focus/focus-ring/`preview*`/`fx*` 一本化＝Button/FAB/Checkbox/Chip+ToolBar 採用、他は leaf ヘルパのみ）。prism before/after 撮影ゲート（+ToolBar/ComboBox）。
  - **✅ #14a = 完了（PR #67 + `v1.20.0`・2026-06-22）。** 詳細プラン＝[`docs/superpowers/plans/2026-06-22-14a-dry-consolidation.md`](superpowers/plans/2026-06-22-14a-dry-consolidation.md)。value-preserving リファクタ（正味 **−111 行**・`swift build` 緑）。**唯一の意図的値変化＝FAB-rest 影 0.30→dp8 0.28**（承認済・#13 doc 予告どおり）。実装クラスタ: ①`ResolvedPalette.bestContrast` public 化→6 widget の private `ink(on:)` コピー廃止 ②`NSView.themeBackingScale` を新規 `Sources/ThemeKit/Shared.swift` に抽出（Family A=byte-identical・Family B=init-time window==nil で値同一・据置 controller=Tooltip/ComboBox/Menu は除外）③共有 free func `layerTxn(animated:duration:)` を `Shared.swift` に（9 widget private 削除・inline snap 3 + `PopupFade.transact` 委譲・**Border snap-only variant は member shadowing で据置**）④inline `Elevation` struct→`palette.shadow(.dpN)`（Button/Group/FAB-pressed 完全一致・FAB-rest dp8 snap・ToolBar formula 据置）⑤`ControlRole`+`ResolvedPalette.color(for:)`（Button/Group/FAB/Chip roleColor + ToolBar/List role arm のみ委譲・**neutral=foreground**・公開 enum 不変）。**⑥`CornerPath` は #14a では見送り**（消費者=ThemedButton 単一・rule-of-three 未達＝DRY 利得ゼロ／selective-corner seam は #14b の `ThemedControl` 基底が 2 番目の消費者になる時に切る＝投機的抽象回避）。検証=`swift build` 緑 + 敵対的レビュー（6 クラスタ × 独立スケプティック + finding 検証・7 agent＝**confirmed defect 0**・cosmetic nit のみ修正）+ Tests（FAB-rest 0.28 + `color(for:)`/`bestContrast` 契約テスト・CI XCTest）。**ライブ目視（prism before/after：Button/FAB/Checkbox/Chip + ToolBar/ComboBox）は maintainer 委譲**（agent 画面収録不可・[[prism-bench]]）。**本丸 `ThemedControl: NSView` 基底（hover/press/focus/focus-ring/`preview*`/`fx*` 一本化 + `CornerPath` seam）は #14b（別 PR・要設計 grill）として暗黙にしない。**
  - **✅ #14b = 完了（PR #68 + `v1.21.0`・2026-06-23）。** 詳細設計＝[`docs/superpowers/specs/2026-06-22-14b-themedcontrol-base-design.md`](superpowers/specs/2026-06-22-14b-themedcontrol-base-design.md)（8-agent 調査 workflow + ブレインストーミング3セクション全 OK で確定）・subagent-driven 実装（6タスク・タスク毎 spec+品質レビュー全 PASS）。**調査が overturn した親 spec の3前提（ユーザー設計承認に織込み済）**: ① `ThemedControl` の基底は **`NSView`→`NSControl`**（4部品ともセルレス NSControl で `_enabled/_target/_action`+`sendAction`+`performKeyEquivalent`+無効化後始末を逐語コピー＝NSView だと再追加の羽目）。② **ToolBar は完全採用に含めない**（コンテナで自前 control 状態ゼロ・per-item hover は子 ThemedButton に `previewHovered` を押し下げるのみ）＝ToolBar/ButtonGroup のコンテナ共通化は将来の別項目「**ThemedContainer 基底**」へ切り出し（2つ目の実消費者が出たら）。③ **`CornerPath` 抽出は据え置き継続**（消費者は今も ThemedButton 1つ＝rule-of-three 未達／基底は focus-ring layer のみ所有・リングの形はサブクラス override）。**採用 = Button/FAB/Checkbox/Chip の4部品のみ**（基底所有＝tracking/状態/mouse trio/FR/Space-flash/focus-ring/`preview*`/`fx*`／seam＝press 2-target・`keyboardActivate`+`flashThenActivate`・二重ゲート `appearanceGate`/`focusGate`・`didDisable`・`positionLayers`・ring 形）。**パート2 トークン配線は実質 focus-ring outset の4重複→基底 `focusRingOutset = Space.xxs(2)` 集約のみ**（#13 が radius/gap 大半を配線済）。**ユーザー判断**: List 密度不変 gap（lineGap/badgeGap/badgeHPad/clusterGap/budgetMargin）は **Metrics 据え置き**。実装プラン＝[`docs/superpowers/plans/2026-06-22-14b-themedcontrol-base.md`](superpowers/plans/2026-06-22-14b-themedcontrol-base.md)（6 タスク・73 ステップ）。**実装結果**: 基底 **+425 / 4部品 −1068 行**・`swift build` 緑・**最終 whole-branch レビュー（opus）= Ready to merge**（Critical/Important 0・4部品すべて pre-refactor とバイト等価を確認・seam 義務2点も全部品に存在・shadowing 無し）・**CI 緑（`swift test` 616 tests / 0 failures＝既存4部品テストがバイト等価の回帰ネット）**。**prism before/after ライブ目視は PR #68 本文のチェックリストで maintainer 委譲**（agent 画面収録不可・[[prism-bench]]）。**将来項目（暗黙にしない）**: ToolBar/ButtonGroup の「**ThemedContainer 基底**」・`CornerPath` 抽出（ともに2つ目の実消費者が出たら）。
- **#15 ヘッドレス純粋核 + 相互作用/a11y = ✅ 完了（PR #69 + `v1.22.0`・2026-06-23）。** **ThemedList 核を先に**抽出→ComboBox/Menu 核が List 核に依存（コンポーズ関係踏襲）。新規 pure leaf `ListCore`（top-level free func: `resolveSelection`/`nextHighlight`/`comboFilter`+`reconcileSelection`/`menuKeyIntent`・Foundation/Sendable・**XCTest は CI のみ＝CLT に XCTest 不在**・spec の "CLT で XCTest 可" は誤りを訂正済）。Tasks 1–5 は byte-identical 委譲（List/ComboBox/Menu）。controlled/uncontrolled seam＝firing setters（`setChecked(_:notifying:)`/`commitSelection(_:)`/`setText(_:notifying:)`・既存 `selectRow`/`selectedID=` 二門と同型）。a11y P0/P1＝`postAXValueChanged()` 共有ヘルパ + value 属性を **committed-change site のみ**で発火（no-flood・ComboBox は inner NSTextField へ転送）。詳細設計＝[`docs/superpowers/specs/2026-06-23-15-headless-cores-seam-a11y-design.md`](superpowers/specs/2026-06-23-15-headless-cores-seam-a11y-design.md)・実装プラン＝[`docs/superpowers/plans/2026-06-23-15-headless-cores-seam-a11y.md`](superpowers/plans/2026-06-23-15-headless-cores-seam-a11y.md)。**実装=subagent-driven 8タスク（タスク毎 spec+品質レビュー全 PASS）+ 9-agent 敵対検証（byte-identical×4・ListCore purity・a11y no-flood すべて UPHELD／holistic+completeness ともに readyToMerge／確定ブロッカー 0・reload AX stale の Minor 1件は本 PR で修正済）・`swift build` 緑**。**実装結果**: `swift build` 緑・**CI 緑（full Xcode `swift test`＝新 ListCoreTests + 既存 ThemeKitTests 回帰ネット）**・squash-merge + `v1.22.0` tagged。**残 maintainer ゲート（VoiceOver 9項目: Checkbox/Chip/ComboBox×4/List/ButtonGroup/TextField 二重発話なし + prism 無ドリフト目視）は merge 後フォロー**（ユーザー判断 2026-06-23＝VO は問題あれば相談・additive a11y のため穴は follow-up PR で可）。**将来項目（暗黙にしない）**: max-a11y P2/P3（RowAX value 昇格・ComboBox row-AX vending・ToolBar labels）・標準 TextField 単体の commit announce・typeAheadMatch/DnD core は ListCore 化スコープ外。

### Phase 2 — sill を SwiftUI 化（A→B）
- **#16 SwiftUI 提供層（A）= ✅ 完了（PR #70 + `v1.23.0`・2026-06-23）。** prism の**ウィジェット橋 14個**を新規公開モジュール **`ThemeKitUI`**（sill で初の `import SwiftUI` 層・deps=ThemeKit/PaletteKit/Effects）に昇格＝Border/Button/ButtonGroup/Checkbox/Chip/Divider/TextField/FAB/Skeleton/ComboBox/Tooltip/List/Menu/ToolBar。各橋は `public struct` + 明示 `public init`（SwiftUI の memberwise init は internal）+ public protocol witness + 必要箇所は public Coordinator・CLT 安全（`#Preview` 不使用）。**prism を最初の consumer に**＝in-tree 橋を削除し `import ThemeKitUI`（ドリフトゼロ）。Effects/PixelArt の**デモ橋**（ParticleFieldView/SplatterFieldView/PixelArt*/HeavyRule/IconBarView…）は prism 据え置き（Effects/PixelArt/Motion を引き込むため除外）。**2橋を一般公開コンポーネントに reshape**（1:1 移動でなく＝[[kit-component-philosophy]]）: `ThemedMenuTriggerView` は caller の `[ThemedMenu.MenuItem]`（+title/trailingSymbol）を受け、`ThemedToolBarView` は public nested `Item` value descriptor を公開（`ThemedToolBar.Item` は `.custom(NSView)` 持ち＝SwiftUI 更新を跨げない）。**改名**（public 面の統一・[[build-best-then-migrate]]）: `ListView`→`ThemedListView`・`MenuTriggerView`→`ThemedMenuTriggerView`・`ThemedFieldView`→`ThemedTextFieldView`。palette は `Color(nsColor:)`/`ResolvedPalette` のまま（動的 appearance・@MainActor resolver）。**palette→Color 便利機能は #17 に先送り**（消費者1つ＝rule-of-three 未達）。**テストターゲット無し**（純 `@MainActor` コード移動・headless ロジックは ListCore/ThemeKitTests が網羅・XCTest は CI のみ）。検証＝`swift build` 緑 + **7観点敵対的レビュー（26 agent・finding 毎に独立スケプティック検証）= 確定 minor/nit 4件修正**（menu items reactivity・死んだ Palette 直接依存除去・Field→TextField 改名・seed-once doc）**・誤検出15件棄却**（clean-move 12橋は挙動バイト等価を確認）+ **CI 緑（full Xcode `swift test` + lint）**。**prism ライブ目視 maintainer ゲートは waive**（ユーザー判断 2026-06-23＝バイト等価移動で描画不変・PR #70 本文にチェックリスト）。**将来（暗黙にしない）**: #17 ネイティブ SwiftUI 部品（B）+ Phase 3 アプリ適用（#16 依存・#17 非依存）。`ThemedMenuTriggerView` の固定トリガ frame・`ThemedToolBarView.Item` の image case 欠如は #17/将来の汎用化で対応可。
### Phase A — sill の SwiftUI 部品を建てる（#17 を再定義・拡張）
> 0ベース audit が出した「各アプリ再構築に必要だが sill にまだ無い SwiftUI 部品」を建てる工程。`ThemeKitUI` を本物の SwiftUI 部品ライブラリへ育てる。番号は仮。各部品は prism で live 検証→アプリが consumer に。**ネイティブ純正で足りない表現は ThemeKitUI 橋（`NSViewRepresentable` で実 AppKit/CG を内包）で実現してよい＝「SwiftUI UI 層＋薄い裏方」。** 各部品の要求元は deep-dive memo の §0.6。
- **#17a Canvas 粒子/スプライト/line-pets システム**（`Effects`→SwiftUI `Canvas` 橋）— ★最優先・最頻出（wand trail/arcade・perch particle・halo ring・facet pet/line-pets が共有）。閉形式 `f(now)` の既存 `Effects`/`ParticleBurst`/`TrailGeometry`/`PixelArt` を SwiftUI Canvas で描く橋＋決定論プレビュー seam。**設計確定（2026-06-23・ユーザー承認）＝ハイブリッド（Canvas-native: 粒子/splatter/trail/border ／ NSViewRepresentable: pixel sprite/line-pets〔antialias-off wall〕）＋`frozen` seam・`ThemeKitUI` へ追加（deps に PixelArt/Trail/Motion）・prism を consumer に。設計＝[`2026-06-23-17a-canvas-effects-bridge-design.md`](superpowers/specs/2026-06-23-17a-canvas-effects-bridge-design.md)。** **✅ 完了: PR #71 + `v1.24.0`・2026-06-23。** 2裁定（2026-06-23）: ① 粒子+splatter も **Canvas 再実装でなく NSViewRepresentable バイト等価昇格**（NSShadow グロー fidelity 優先＝spec の Canvas-native から意図的逸脱・ユーザー裁定）。② **build-best**＝下層ドロー関数の汎用性を公開（path/emitter/sprite/icon を入力化）でなく prism デモの逐語コピーにしない（ユーザー指摘で修正）。**昇格6部品**: `ParticleBurstView`/`InkSplatterView`/`PixelSpriteView`/`LinePetsView`/`PathPetView`/`ChompCorridorView`＋共有 `EffectClock`。catalog デモ（pac+ScaleTier ladder）は消費アプリ無し→prism ローカル `PacLadderView` 据え置き（Trail/Motion/border-strip viz も同）。**ThemeKitUI 依存に足したのは Motion+PixelArt のみ**（Trail は Effects 内＝spec の「Trail 追加」を訂正）。検証＝`swift build` 緑＋敵対的バイト等価レビュー（6部品＋構造・独立検証＝確定欠陥0・nit1修正）＋**CI 緑（full Xcode `swift test`＋lint）**＋**prism ライブ目視 PASSED（ユーザー 2026-06-23＝透明背景化の残像なし・レイアウト・グロー確認）**→ squash-merge + `v1.24.0`。実装プラン＝[`2026-06-23-17a-canvas-effects-bridge.md`](superpowers/plans/2026-06-23-17a-canvas-effects-bridge.md)。**将来（暗黙にしない）**: path/トリガ等の最終 API fitting は Phase B の各アプリ消費時に refine／Trail/Motion/border-strip viz の公開化は実消費者が出たら。
- **#17b `ThemedList` 強化** — キーボード nav（`onKeyPress`: ↑↓/Space/Return/Esc）・折りたたみ section header（アニメ）・multi-select（複数行ドラッグ）・drag-ghost overlay ヘルパ・sticky header 強化。facet tree/grid・wand launcher が要求（ListCore は元々 facet tree をモデルに設計＝最有力消費者）。
- **#17c `BlurBackdropView`** — `NSVisualEffectView`(.menu/.hud)＋mask の SwiftUI 橋。wand/perch/glance が要求。
- **#17d `AnimatedBorderView`** — 虹/グラデの animated border（`ThemedBorder`/`Effects` 駆動）。wand 枠・facet chrome が要求。
- **#17e サムネ Grid 部品** — ScreenCaptureKit 画像を並べる themed grid（facet grid/rail）。
- **#17f `MarkdownKit`** — glance 用: `AttributedString` 橋・**GFM 表（`LazyVGrid`・sticky/CJK 対応）**・角丸 code block/blockquote Shape・Highlightr 橋。glance の唯一の表組み課題を sill 側で解く道（薄 `NSTextView` backend を採るなら本項目は任意）。
- **#17g 小物** — `ThemedPill`・cascade/radial menu placement・search-pill badge・`DisplayLink`→Combine ユーティリティ。
- **決定論プレビュー seam**（各部品）＋**アニメ/見た目は maintainer 実機確認ゲート**（[[chomp-push-gate]] 同型・agent は画面収録不可・[[prism-bench]]）。

### Phase B — 各アプリ UI を 0ベースで SwiftUI/sill 再構築（各 app repo follow-up）
> 旧 Phase 3 を破棄して再作成。各アプリの UI を**ゼロから** SwiftUI/sill で組み直す。ネイティブは UI でなく**裏方**（ScreenCaptureKit=画像・CGEventTap=入力・SkyLight=窓位置・AX=データ）に限局し、`NSHostingView` で SwiftUI を載せる。各パーツの設計/裏方/要追加部品＝deep-dive memo §1–§5。**順 = 確実な pilot から**（旧「facet 最後」は破棄）。
- **#18 facet pilot — search-bar → tree**（pilot・effort S→M）: search-bar は ThemeKitUI `TextField` 即適用（IME 実証済）。tree は `ThemedList`＝#17b の実地検証。SF Symbols→sill phosphor 収束も同時。**ここで SwiftUI-on-sill の seam を実証**してから横展開。
- **#19 halo**（単純・早期検証向き）: リング+glow+line-pets を SwiftUI Canvas(#17a)。裏方=SkyLight(位置)。pure `Effects` 消費の理想形。
- **#20 wand**: launcher を SwiftUI メニュー/グリッド・trail/arcade を Canvas(#17a)・枠を `AnimatedBorder`(#17d)・blur(#17c)。裏方=CGEventTap。**+ `NSColorParse`→`PaletteKit` 収束（pure）**。
- **#21 perch**: hint pill/grid を `ThemedPill`(#17g)+Canvas(#17a)・search を本物の検索 UI に。裏方=CGEventTap/AX/OverlayCoords・**never-key は窓属性（裏方）で両立**。
- **#22 glance**（最後・#17f 依存）: markdown を `MarkdownKit`(#17f)（or 薄 `NSTextView` backend）・chrome を SwiftUI（key≠active は実証済）。
- **2つの注意（hard wall ではない）**: facet pet のアンチエイリアス OFF ドット絵＝小さな `NSViewRepresentable` blitter を残す／glance GFM 表＝#17f or 薄 `NSTextView`。

### 順序・依存（要点）
- **Phase A（sill 部品）→ Phase B（アプリ再構築）。** Phase A 最優先 = **#17a Canvas 粒子系 ✅ 完了（PR #71 + `v1.24.0`）**（5アプリ横断）＋**#17b `ThemedList` 強化**（facet pilot の前提・次）。
- Phase B は **facet pilot（#18 search→tree）で seam を実証**してから横展開。halo(#19) は単純で早期検証向き。glance(#22) は #17f 完成後。
- pure 収束（wand `NSColorParse`→`PaletteKit` 等）は Phase 横断で随時。
- **破棄**: 旧クリティカルパス「#13→#15→#17」・「ハイブリッド（AppKit 殻＋中身だけ SwiftUI）」・「Phase 3＝pure 収束のみ」。0ベースでは UI 全体が SwiftUI/sill。

## 🧭 現在地（引き継ぎ — 最新セッション 2026-06-21）

**新セッションはまずここを読む。** 進捗の単一ソースはこのファイル。

- **#1（SVG アイコン基盤）= ✅ 完了（PR #52 merged + `v1.8.0` tagged 2026-06-19）。** 内容: SwiftDraw（`<0.25` ピン）・Phosphor/Simple Icons を使う分だけ vendor・`phosphorImage`/`simpleIconImage` ローダ・共有 `tintedBitmap`（SF は byte-identical）・新画像API（`ThemedButton/FAB.leadingImage`・`ThemedToolBar.ButtonItem.image`）・prism「Icons」タブ＋`family` 設定キー。敵対的レビュー反映済。詳細は下の #1 と Package.swift / `Sources/ThemeKit/Resources/README.md`。
- **アイコン vendor 方針 = A「使う分だけ」で確定**（全カタログ ~12.5k/~6MB は入れない。発見可能性は README＋DEBUG ログ＋CLAUDE.md で担保済。#2 の sweep で実使用分が自然に増える）。
- **#2（全 SF→Phosphor sweep）= ✅ 完了（PR #53 merged + `v1.9.0` tagged 2026-06-19）。** SF Symbol を Sources/ **と Tests/** から全廃（`*Symbol: String?` は型維持で Phosphor スラッグを受ける／内部リゾルバを `phosphorImage` に差し替え）・メニュー✓は `check`(bold)・prism 全 showcase の文字列＋`Image(systemName:)`→`phosphorIcon` ヘルパ・Phosphor regular SVG を 31 個 vendor。`swift build`＋prism ライブ撮影（text/action/collection/chrome 両 tint 経路）＋敵対的レビュー（指摘8件反映）＋CI `swift test`（full Xcode・385 tests）緑。**→ これで wand 移植が解除**（以降は wand リポで管理）。⚠教訓: sweep は **Tests/ も含める**（`swift test` は CLT ローカル不可＝CI だけが守るゲート。Button/FAB は icon 解決依存で nil slug→赤、TextField/ToolBar は文字列有無で確保）。メモリ [[sweep-include-tests]]。
- **#3（`ThemedChip` 新規）= ✅ 完了（PR #54 merged + `v1.10.0` tagged 2026-06-20）。** MUI `<Chip>`＋HTML `<kbd>` を1部品に。`public final class ThemedChip: NSControl`・`variant{filled,outlined,keycap}`・`size{small24,medium32}`・`role{neutral,primary,secondary,error}`・`title`/`leadingSymbol`/`leadingImage`/`isSelected`/`onTap?`(クリック可)/`onDelete?`(末尾×=`x-circle`)/`preview*`。perch のヒント**オーバーレイは設計上スコープ外**（`OverlayCanvas.PillLayout` を CG ループで N 個 bespoke アニメ描画＝NSView 部品で代替不可）。実装＋prism showcase＋29 XCTest＋`swift build` 緑＋3テーマ ライブ撮影＋敵対的レビュー（5観点×2票・確定1件=`onTap` の動的 a11y role 再同期を修正）＋CI `swift test`（full Xcode）緑。⚠教訓: **テストは CI だけが守る**＝初回 CI で `laidOut` の trailing-closure 引数順 compile error を捕捉（`swift test` は CLT ローカル不可）。メモリ [[sweep-include-tests]]。
- **#4（perch の自前エフェクトを sill の既存 Effects に寄せる）= ✅ 完了（perch PR #132 merged 2026-06-20・sill 変更なし）。** perch のネオン枠を自前 hue テーブル（`BorderEffect.baseHex`＋`rotateHue`＋`borderHueOffset`）から sill `Effects` に収束＝`borderEffectFor`→`resolveBorder`/`blendThrough` 委譲。perch は redraw クロック所有＋NSColor 化＋glow 合成を維持（sill の app 側 border 契約どおり）。決定（ユーザー Q1/Q2 2026-06-20）= **sill をそのまま採用**（neon/cyber/vapor/kawaii は facet と同じ list-blend に・rainbow 実質同じ／animatedPalette 結線は後回し）。実装: sill ピン 0.11.0→1.10.0（Palette/ConfigSchema/CLIKit はバイト一致＝ノーオペ）・`Effects` を PerchAdapterMacOS にリンク・`.random` を1回解決（draw 毎フリッカも解消）・`BorderEffectMappingTests`（perch↔sill 名前ドリフトを CI で検出）。CI 緑（swift build＋full Xcode swift test）。⚠ perch は headless＝視覚自動テスト無し（見た目は `./run.sh` 手動）。⚠教訓: **#4 は sill 無改変＝アプリ移植のみで完結**（build-best-then-migrate＝sill は既に best、適用は app repo の follow-up。wand 移植と同型で、以降の perch 作業も perch リポで管理）。
- **#5（perch の自前の色計算を sill の PaletteKit に寄せる）= ✅ 完了（perch PR #133 merged 2026-06-20・sill 無改変）。** #4 と同型＝**sill 無改変**（build-best-then-migrate）。`HintPainter.resolvePalette` を `PaletteKit.resolve(spec)` 経由に＝role 色（accent/text/miss/font）＋ system-primary sentinel（0→`controlAccentColor`）を sill が materialize。**要設計だった 3 perch-ism の裁定**（ユーザー Q 2026-06-20 = ③は perch 側に残す）: ① sentinel→**PaletteKit が吸収済**（perch の手書き削除）・② translucency（pillBgAlpha・frosted +0.45）→**フィールドは spec に既存・描画は perch 据置**・③ per-app `[overlay].accent` override→**perch 側に残す**（resolve 後に重ねる・sill は per-app accent を設計上持たない）。重複 `color(hex:)`（HintPainter＋ParticleDriver）を PaletteKit の `NSColor(hex:)` に寄せて全廃。**全テーマで byte-identical**（perch が渡す spec は常に `.fixed`＝"system" は perchSystemSpec で intercept・組み込み/custom は背景あり・`parseHexValue` が alpha を捨て全 role opaque）。`swift build` 緑＋敵対的レビュー（色保存・網羅性を独立確認）＋CI 用 `PaletteResolveMappingTests` 追加（4ケース・perch headless＝`swift test` は CI だけ）。**CI 緑（build+full Xcode swift test=76 tests／lint）→ squash-merge 済（ユーザー「推奨でOK」2026-06-20）。** ⚠教訓: #4・#5 とも sill 無改変＝app 移植のみで完結（sill は既に best）。以降の perch 作業も perch リポで管理。**→ #6 ✅ 完了（下記）・次は #7。**
- **#6（`ThemedTransition` 新規＝アニメ用の計算）= ✅ 完了（PR #55 merged + `v1.11.0` tagged 2026-06-20）。** 新規 **pure モジュール `Motion`**（型 `ThemedTransition`）＝ファミリーの **一発（one-shot）** アニメ計算。`Effects`（**巡回**＝border/rainbow/pets）の対で、住み分けは Motion=play-once（slide/fade/pop/reorder）。perch/facet/wand が十数箇所でハンドロールする `min(elapsed/duration,1)`・`1-pow(1-p,3)`・duration マジックナンバー・spring を1つの**純粋・Sendable・AppKit-free** 語彙に集約（= MUI `theme.transitions` の native 移植・**clock はアプリ所有**＝sill の f(now) 流儀）。内容: **Duration**（snap/exit/enter/move/emphasis/staggerStep＝**実測 0.10–0.22s 帯に校正**・MUI の梯子は不採用）・**Easing**（power系＋Material cubic-bezier を WebKit UnitBezier solver で厳密・`spring`・custom）・**Tween**（start/duration/delay/easing を1値＝毎フレーム sample）・`lerp`(Double+CG)・`spring`(facet verbatim 昇格)・`dampedSine`(perch/wand)・`autoDuration`(MUI)・`scaled`。検証: `swift build` 緑・prism「**Motion**」タブ live 撮影（terminal/github-light/system vibrancy）・`MotionTests`・**敵対的レビュー**（4観点×独立検証＝**CI-only テストの float 境界バグ1件を発見+修正**・他は正しいと確認）・**CI 緑（full Xcode `swift test`=445 tests／build／lint）→ squash-merge 済**。⚠教訓: Tween の `isComplete` 境界テストは 0.1/0.2/0.3 だと `100.3-100 < 0.1+0.2` で CI だけ赤に→**2進で厳密な 0.25/0.5/0.75 に校正**（`swift test` は CLT 不可＝CI だけが守る）。メモリ [[sweep-include-tests]]。**アプリ移植(perch/facet/wand)は follow-up**（build-best-then-migrate・本PRは sill のみ・additive）。
- **#7（`ParticleBurst` 新規＝紙吹雪/花火を `Effects` に）= ✅ 完了（PR #56 merged + `v1.12.0` tagged 2026-06-20）。** 一発バーストを **border flash と同型**に＝`rollBurst`（トリガで初速・色・回転・寿命をロール＝`rollFlash` の対）→`resolveParticles`（**閉形式** `x=x₀+v·t`／`y=y₀+v·t+½g·t²`／flutter `sway·sin`／`alpha=1−t/(dur·life)`／`rotation=spin·t`・per-particle `life` で有機的に溶ける）→AppKit `drawParticles`（グロー spark＋hot core／エッジフリップする紙片＝`drawLinePets` の対）。**Euler 積分なし・mutable state なし・`f(now)`**（perch の `dt` 積分より正確・replayable）。pure 層は Sendable・UInt32/Double（CoreGraphics は `CGPoint` overload、AppKit は draw を `#if canImport` gate）。emission `.fireworks`(放射・gravity360)/`.confetti`(ポッパー＝上方初速・gravity900)・色は `EffectSpec` パレット＋accent から・`EffectIntensity` で count/reach（hard-cap 6…40/emitter）。perch `ParticleDriver` を一般化（wand burst も将来吸収）＝**アプリ移植は follow-up**（build-best-then-migrate・本PRは sill のみ additive）。prism「**Particles**」タブ＝花火＋紙吹雪を**実 `drawParticles`** で全テーマ live（`PRISM_PARTICLE_T` freeze で決定論撮影）。検証: `swift build` 緑＋prism ライブ撮影(synthwave)＋**敵対的レビュー**（4観点×独立検証＝freeze stale-bounds 等4件 refuted・nit/minor 2件反映）＋**CI 緑（build／full Xcode `swift test`／lint）→ squash-merge 済**。⚠教訓: 初回 CI が**テスト専用 compile エラー**を捕捉＝`rollBurst(from: [])` 空配列リテラルがタプル/CGPoint 2オーバーロードで ambiguous（`swift build` は test 非 compile＝CI だけが守る）。メモリ [[sweep-include-tests]]。**→ #8〜11 は順番入れ替え可（#9 完了済・残り実行順 #11+#10 一括 → #8）。**
- **#9（wand effect 一式を sill へ）= ✅ 完了（スコープ=full・ユーザー裁定 2026-06-21）。** 調査 workflow で確定: line-pet/chomp pet は**既に共有済**、burst は **#7 `ParticleBurst` と同形**（perch＋wand で rule-of-three 達成）、trail/gesture は**新規・前方投資**、`ChompRenderer`(~1000行アーケード) は wand 据え置き。**連作 PR で sill 側を先に**: **9a** `ParticleBurst` 拡張(`radiusSpeed`縮小)→ **9b** `SplatterShape`(ink splatter・新規 pure＋AppKit＋prism)→ **9c** trail ジオメトリ(`resampleAlongPolyline`＋`roundedCornerPath` を Effects に・色は `blendThrough`)→ **9d** 新規 pure モジュール `Gesture`(`recognize`/`Direction`/`Sample`/spec・CLIKit 型 mechanism-only・logic-only テストのみ)。詳細は下の項目 9。**9a ✅（`radiusSpeed`・PR #57+`v1.13.0`）・9b ✅（`SplatterShape`・PR #58+`v1.14.0`）・9c ✅（`TrailGeometry`・PR #59+`v1.15.0`）・9d ✅（`Gesture`・PR #60+`v1.16.0`・敵対的レビュー fidelity/purity/tests=0 件）。9e（wand 本体移植）= 不要＝ユーザー裁定 2026-06-21（wand repo 側 follow-up なので sill ROADMAP では追わない）。→ sill 側 #9 完結。**
- **⏭ 引き継ぎ（全 #1–#12 完了 — 残作業なし）:** **#9 完結（9a–9d 済・9e 不要）。#11+#10 = ✅ 完了（PR #61）。#12 完了 = ChompCorridor（PR #62 + `v1.17.0`）。** ✅ **#8 完了 = MUI-grounded 内部 TypeScale + font-fidelity 修正（PR #63 + `v1.18.0`・2026-06-22）→ 下の #8 セクション参照。ライブ目視はユーザーに委譲（実装時 2nd セッションが prism 使用中）。**
- **#10+#11（prism の app 別タブ＋エフェクトトグル移設）= ✅ 完了（PR #61・2026-06-21・prism のみ＝バージョンタグ無し）。**
  - **裁定 = Option A**（ユーザー 2026-06-21）＝ウィジェット **Kit タブは維持**し、単一 **Chrome タブを app 別**（facet/wand/perch/halo/glance）に分割。各 app タブ = その app の chrome モック＋「実消費」caption。全テーマ grid は維持（per-app テーマ絞り込み=Option B は不採用）。
  - **🔑 調査の決定的発見**（5 app 並列 workflow）: **各 app は ThemeKit ウィジェットをほぼ使っていない**（facet が `ThemedScroller` 1個のみ・wand/perch/halo/glance は 0）。実体は「**テーマ(Palette)＋エフェクト(枠/フラッシュ/粒子/ペット)で彩色した独自 chrome**」。→ Kit タブ=ライブラリ showcase（build-best-then-migrate の前方投資）、App タブ=consumer 実態、と住み分け。
  - **halo を追加（5 app）** ＝ ROADMAP 表記の 4（facet/wand/perch/glance）に対する**意図的逸脱**。halo は実在の family GUI app かつ border の最も純粋な消費者で、ユーザー承認の Option A 選択肢に含めて提示済。新規 **MockHalo**（focused-window ring = 既存 `ThemedBorderView`・`HaloShowcase.swift`）。
  - **#11** = EffectToggle を family タブ列末尾 → **タイトル行（右上）** へ移設（エフェクトはテーマ軸の制御＝タブ列から分離）。タブ列は **Kit / Apps の2グループ**（各 `FlowLayout` で wrap）に。
  - 実装: `KitFamily` を kit/app 2グループ化（`group`/`kitCases`/`appCases`・`.chrome` 廃止）＋`AppChrome` caption テーブル＋ヘッダ2軸＋カード switch 5 app ケース＋`appCaption`。**検証**: `swift build` 緑（prism は **テスト対象なし**＝[[prism-bench]] ライブ撮影で検証・撮影4枚で halo リング glow／facet 全テーマ MockTree／Effects ON·OFF を確認）＋**敵対的レビュー3観点**（Swift/sill/完全性＝1件 appCaption の per-frame 再 resolve を修正）。⚠教訓: prism-only 変更は **ライブラリ製品の変更なし＝バージョンタグ無し**（タグは library change のときのみ）。プラン → [`docs/superpowers/plans/2026-06-21-prism-app-tabs.md`](superpowers/plans/2026-06-21-prism-app-tabs.md)。

## #12 — wand chomp の sill 再解釈「ChompCorridor」（✅ 完了・PR #62 + `v1.17.0`・2026-06-21〜2026-06-22）

> **設計確定（ユーザー承認済）。** 詳細設計 → [`docs/superpowers/specs/2026-06-21-chomp-sill-reinterpretation-design.md`](superpowers/specs/2026-06-21-chomp-sill-reinterpretation-design.md)。
> ⚠ **push ゲート**: chomp は **ユーザーが sill でライブ確認してから push**（commit/branch は自由・後で「push OK」も安易に信じず「sill で確認済み？」と再確認）。通常の green-CI→自動 squash-merge を chomp に限り上書き（メモリ chomp-push-gate）。**作業ディレクトリ = `sill/` 本体・ブランチ `feat-chomp-12`（origin/main から先行）。⚠ 2026-06-22 のクラッシュ後、混乱回避のため別 worktree `sill-wt-chomp` は廃止し単一ディレクトリ運用に統一（ユーザー指示）。**
> ⚠ **マージ保留（ユーザー指示 2026-06-21「マージは全部完了してから!!!」）**: #12 は **全フェーズ（Ph2…Ph5）が完了するまで main にマージしない・フェーズ毎の merge/tag をしない**。`feat-chomp-12` に積み続け、**#12 完了時に一括 squash-merge ＋ 最終タグ 1 本**（フェーズ毎の v1.17/v1.18… 目安は廃止＝最後にまとめて版を付ける）。各フェーズはライブ検証 PASSED でも「完了＝マージ可」にしない。

- **目的**: wand の chomp（パックマン風アーケード）を sill の語彙で**見た目を再現**（移植でなく sill 解釈）。完成度が高ければ wand へ backport（wand リポ側 follow-up・sill ROADMAP では追わない＝旧 9e と同型）。
- **確定判断（ユーザー Q&A 2026-06-21）**: ① 到達点 = **フル再現（迷路まで）** ／ ② 色 = **固定アーケード色（常にパックマン）**＝テーマ不変・スプライト内部色も intrinsic 定数 ／ ③ 既存 line-pet との関係 = **統一（ピクセルに寄せる）** ／ ④ 解釈の自由度高（ドット絵の雰囲気が残れば独自 OK・ピクセル完全一致は不要）。
- **アーキテクチャ**: 新規 pure モジュール **`PixelArt`**（`PixelSprite`＝文字列グリッド→セル・`pacManCells`＝円−口楔・`positionHash01`・`ScaleTier`）＋ **`Motion.frameStep`**（離散スプライト差し替え）＋ **`Effects` 拡張**（`drawPixelSprite`・正規スプライト 3 種・`drawChompCorridor`＝2 ストロークネオン壁＋内角フィレット＋ペレット列＋追従パックマン＋不一致幽霊・`eatCrossed` 食べ判定・虹フラッシュは `EffectSpec.chomp` 再利用・「+N」は Motion）。clock はアプリ/prism 注入（`f(now)`）。再利用: `Gesture`(#9d)・`TrailGeometry`(#9c)・`ParticleBurst`/`SplatterShape`・`Palette.chomp` 色。
- **フェーズ（各 additive・prism live 撮影で検証。⚠ フェーズ毎の ship/merge/tag はしない＝全 Ph 完了で一括 merge＋最終タグ 1 本＝ユーザー指示。下の各行末「vX.Y.0 目安」は廃止＝最後にまとめて版付け）**:
  - **Ph1** `PixelArt`（`PixelSprite.cells()`＋`pacManCells`＝円−口楔＋`mouthHalfRad`＋`positionHash01`＝負座標安全 bitPattern＋`ScaleTier` 2/3/4.5）＋ `Effects`（`drawPixelSprite`/`drawPacMan`＝antialias OFF・AppKit gate＋正規スプライト 3 種=cherry 12×13・ghost/ghostAlt 14×14＋intrinsic `SpriteColor`＝chomp ロール一致）＋ prism カード（`.particles` タブ・**実描画を NSViewRepresentable で host**）＋ XCTest 2 本。**🚧 実装完了（ローカル `swift build` 緑・スプライトを offscreen NSImage レンダで視覚確認＝パックマン/チェリー/ゴーストと認識可・敵対的レビュー4観点反映）・未 push＝ユーザーの sill ライブ確認待ち**（v1.17.0 目安）
  - **Ph2** 既存 line-pet をピクセル統一＋`Motion.frameStep`（**検証ゲート**: 小サイズで崩れないか・最悪 line-pet は滑らか維持に差し戻し可）。**✅ 実装完了＋ライブ検証 PASSED（ユーザー「全身OK」2026-06-21＝口パク/waddle/線ペット周回すべて確認）。commit `fa2d1f6`＋ゴースト `b42748b`。⚠ ただし上記マージ保留指示によりマージはしない＝#12 完了まで `feat-chomp-12` に保持**。➕ **ゴースト改良 commit `b42748b`**: ユーザー素材 `全身.gif` を ASCII 化して `CanonicalSprite.ghost`/`ghostAlt` を **canonical 大きい目の Blinky（右向き・2 フットポーズ）** に差し替え（旧 = 目が小さく「少し違う」→ 4 幅白目＋2×2 瞳に。同 14×14・同構造でテスト/ビルド緑・offscreen レンダで確認済）。
  - **Ph3** **PathPet**（任意ジェスチャー線を歩く・追従パックマン・不一致幽霊）＝初の「動く」カード。**第一片: directional-eye 直立ゴースト = ✅ 実装＋ライブ確認済（feat-chomp-12・commit・**未 push/未マージ**＝ゲート継続）。ユーザーが prism ライブで4方向承認（2026-06-22「今の状態でOK」）。** 内容: 新規 `GhostLook{up,right,down,left}`＋`facing(dx:dy:)`（tangent→cardinal・y-up）／`CanonicalSprite.ghostSprite(feet:look:)`＝`lookRows(look)`(頭 rows0–11)＋`feetRows`／`ghostFrames(look:)`（4方向×2ポーズを静的キャッシュ）。**目の作り（ユーザーと prism でライブ微調整・参照 `目.gif`/`全身2.gif` 準拠）= 視線方向に目を寄せる**: 上=瞳が白目の上端・下=下端（上下は鏡像）・左右=瞳を外端で縦中央（白が上下に＝斜め見えを解消）。**頭の赤シルエットは全方向で同一**（丸み維持＝崩さない）。`drawLinePets` を per-pet transform 化（ghost は rotate せず**直立**＋`GhostLook.facing(cos rot,sin rot)`、pac は従来通り rotate）。検証: `swift build` 緑＋**offscreen PNG レンダ**（4方向＋全グリッド14幅＋**下辺で逆さにならない**＝直立を実証）＋prism「directional ghost」ストリップ追加＋ユーザー prism ライブ承認＋敵対的レビュー3観点（全て nit・**must-fix 0**）。実行プラン = [`superpowers/specs/2026-06-21-chomp-ghost-directional-eyes-plan.md`](superpowers/specs/2026-06-21-chomp-ghost-directional-eyes-plan.md)。**Ph3 本体 PathPet = ✅ 完了。実装＋ライブ確認済（WIP「Good」→ hardening 後の最終ビルドも「OK」2026-06-22）＋敵対的レビュー(4観点・must-fix 0)＋テスト/helper 補強。⚠ #12 マージ保留により未マージ・未 push＝`feat-chomp-12` に保持。下記引き継ぎ参照。**
  - **Ph4** ネオンコリドー（2 ストローク壁＋内角フィレット）＋ペレット列＋cherry/icon 分け＋ScaleTier。**✅ 完了＝実装＋ライブ確認 PASSED（ユーザー「このままでOK」2026-06-22）。commit `f23df9a`（実装）＋レビュー反映 hardening コミット。⚠ #12 マージ保留により未マージ・未 push（`feat-chomp-12` 保持）。** 新規 `Effects.drawChompCorridor(path,now,valid,tier,scale,speed,icon)`＝同一 `roundedCornerPath` を太い青→細い黒で 2 ストローク（壁バンド・頂点アーティファクト無し）＋`interiorCorners`(純粋・CI テスト) の凹角に黒フィレット円＋`resampleAlongPolyline`×`positionHash01` で中央ペレット列(cherry<0.04/icon<0.08/dot・先頭除外)＋**Ph3 `drawChompPath(showGuide:false)` 再利用**で追従 pac/パニック ghost。`tier`=アーケード寸法・`scale`=解像度。prism「Neon Corridor」ライブカード(直交 90° 迷路＝新規 `orthogonalMazePath`・valid/mismatch)。下の引き継ぎ節に詳細。
  - **Ph5** 食べ判定＋虹フラッシュ＋「+N」スコア = **合成 `ChompCorridor` 完成**・全ループ大カード。**✅ 完了（ライブ確認 PASSED・ユーザー本採用 2026-06-22・PR #62 + `v1.17.0`）= `swift build` 緑＋オフスクリーン PNG で食べ/フラッシュ/+N/正立を確認＋敵対的レビュー4観点 must-fix 0＋↺ replay ボタン/mismatch dots-only のライブ feedback 反映。** 実行プラン [`2026-06-22-chomp-ph5-composite-corridor.md`](superpowers/plans/2026-06-22-chomp-ph5-composite-corridor.md)。詳細は下の引き継ぎ節。
  - **Ph6**（任意・**wand リポ側**）完成度が高ければ backport
- **進捗**: ✅ 計画セッション（spec＋項目登録）＋ **Ph1 実装（feat-chomp-12・commit 済・未 push）**。Ph1 = 上記すべて実装＋`swift build` 緑＋スプライト offscreen レンダ確認＋敵対的レビュー（geometry/architecture/tests/spec の4観点 workflow＝distribution/orientation は green と独立検証・**must-fix 2件修正**＝① reconciliation テストを `ThemeSpec.chomp` 実値照合に・② prism カード高さを `*uiScale` 追従に）。⚠ **agent 環境は画面収録権限なし**＝`screencapture` が真っ黒＝prism ウィンドウ撮影はユーザー側（offscreen NSImage レンダで絵柄は確認済）。⚠ **push ゲート**: prism の PixelArt カードをユーザーが sill でライブ確認するまで push しない（[[chomp-push-gate]]・後で「push OK」も「sill で確認済？」と再確認）。**ユーザーが見た目を承認（2026-06-21「いいと思う」）。ただし main マージは保留（ユーザー明示指示）・push もゲート継続**＝当面は `feat-chomp-12` にローカルコミットを積む（Ph2 以降も同ブランチにチェーン・マージ/タグはまとめて後日）。**次は Ph2**（既存 line-pet をピクセル統一＋`Motion.frameStep`＝口パク 5Hz `[0,0.5,1,0.5]`＋幽霊の脚 waddle・検証ゲート付き）。未達成は各フェーズ行で明示し暗黙にしない。✅ **Ph2 実装完了（feat-chomp-12・commit 済・未 push）**: ① `Motion.frameStep(now:hz:frames:)` 追加（離散サンプラ・floor+総和 modulo で負 now 安全・境界テストは 2 進厳密値で校正）／② `drawChompPet`/`drawGhostPet` をピクセル版に置換（`drawPacMan`/`drawPixelSprite` 経由・口は frameStep 5Hz・幽霊は `CanonicalSprite.waddleFrames` を `waddleHz`=1.5 で swap・ゴーストは row0=ドーム上になるよう y フリップ・旧滑らか版は `drawChompPetSmooth`/`drawGhostPetSmooth` として温存＝ゲートで差し戻すなら呼び先を 1 行戻すだけ）／③ `Effects→Motion` 依存追加（acyclic・Motion は純粋 leaf）＋ canonical 定数を 1 箇所化（`PixelArt.chompMouthFrames/Hz`・`CanonicalSprite.waddleFrames/Hz`）／④ prism PixelArt カードをライブ化（内蔵 60Hz timer・口パク/waddle 動作・`PRISM_CHOMP_T` で freeze）＋**線ペット周回カード追加**（実 `drawLinePets` を小スケールで周回＝検証ゲートの目視用）／⑤ Tests = MotionTests `FrameStepTests`・PixelArtTests `ChompMouthAnimTests`・EffectsTests 線ペット smoke。⚠ **agent は画面収録権限なし＝アニメ確認はユーザー必須**＝検証ゲート（小サイズで崩れないか）はユーザーの sill ライブ判定待ち。
- **✅ #12 完了（Ph1–Ph5 ライブ確認 PASSED・ユーザー本採用 2026-06-22）:** `feat-chomp-12` を **PR #62 で一括 squash-merge ＋ `v1.17.0` タグ**。push・merge ゲートはライブ確認 PASSED をもって解除。残作業なし（残るは別件 #8）。 まず `git fetch` ＋ 本 #12 ＋ 設計 spec ＋ Ph5 プラン [`2026-06-22-chomp-ph5-composite-corridor.md`](superpowers/plans/2026-06-22-chomp-ph5-composite-corridor.md) を読む。作業は `sill/` 本体・ブランチ `feat-chomp-12`（**未 push/未マージ**・origin/main から先行。⚠ クラッシュ後 worktree `sill-wt-chomp` は廃止＝単一ディレクトリ運用）。⚠ **マージ保留**: #12 全フェーズ完了まで merge/tag しない（上記指示）。Ph2–Ph5 は完了/実装済でも触らずブランチに保持。⚠ **push ゲート継続**（[[chomp-push-gate]]）＋ agent は画面収録不可＝ライブ確認はユーザー（[[prism-bench]]・prism は `particles` タブの「ChompCorridor (#12 Ph5)」カード・`PRISM_CHOMP_T=1.2` で凍結）。
  - **✅ 完了＝Ph4 ネオンコリドー（ライブ確認 PASSED「このままでOK」2026-06-22）。** commit `f23df9a`（実装）＋プラン doc `38645ed`＋レビュー反映 hardening（フィレット/icon コメント）。実装詳細は上の Ph4 行。**敵対的レビュー4観点 workflow（geometry/fidelity/determinism/tests・各指摘を独立スケプティック検証）= 生5件→確定後の実 must-fix は 0**: ① 「icon が上下逆」(geometry) = **誤検出と実証して棄却**（オフスクリーンの上向き矢印レンダで `NSImage.draw` が非フリップ host で自前正立と確認・提案フリップを足すと逆さになる＝`drawCorridorIcon` にコメント焼込み済「don't fix this」）／② フィレット半径 spec`0.5` vs 実装`1.15`（must-fix 1＋should-fix 2 が全部これ）= **調整値・ユーザーがライブで `1.15` を承認**（round join で角が既に丸く 0.5 は不可視＝コメントで spec 差を明記）。検証 = `swift build` 緑＋オフスクリーン PNG(valid/mismatch 全部正立)＋ユーザー prism ライブ承認。
  - **✅ Ph5 完了（ライブ確認 PASSED・PR #62 + `v1.17.0`・ユーザー本採用 2026-06-22）= 合成 `ChompCorridor` 完成。** 決定論モデル = **「完全 f(now) 派生」（ユーザー承認 2026-06-22）**＝ペレット消滅/壁フラッシュ/+N を全部 `now` から純粋導出。⚠ spec §3.3 の「`rollFlash`/`resolveBorder` 再利用・`onEat:` 前提」から**意図的に逸脱**: `rollFlash` は RNG で凍結/決定論を壊すため**不使用**（代わりに `EffectSpec.chomp.flash` を `blendThrough` で時間駆動＝虹フラッシュ）・`onEat:` コールバックは**不採用**（フレーム状態が freeze 不変条件と衝突）→ 実アプリの離散イベントは公開純粋 `eatCrossed` を各自の prev/cur 顔弧長と合成。**実装（commit 群・`feat-chomp-12`）**: ① 純粋 `eatCrossed(arc:prev:cur:)`(Trail.swift・半開 `(prev,cur]`)／② `PixelArt.chompBonusPool [100…5000]`＋`bonusValue(x:y:)`(swapped-coord hash で <0.08 選定帯と非相関)／③ 新規 `Effects/CorridorEat.swift`＝純粋 `ScorePop`/`chompFlashPhase`/`chompScorePops`＋定数 `chompEatFlashDur 0.45`/`chompScorePopDur 0.8`＋AppKit `drawScorePop`(Motion `easeOutCubic` rise+fade)／④ `drawChompPath` に `showHead`(既定 true)＝コリドーは `false` で追従ヘッド光点を外す／⑤ `drawChompCorridor` 統合＝顔弧長 `pathPetCursors().pet` で「通過ペレット消滅(valid のみ)・bonus 食べで壁が `chomp.flash` 色＋グロー強め・+N 浮上」、wrap で全ペレットリスポーン。prism「ChompCorridor (#12 Ph5)」大カードに昇格。Tests = TrailTests `eatCrossed`・PixelArtTests `bonusValue`・新規 EffectsTests `CorridorTests`(flash/score 純粋関数＋draw スモーク・境界 now は 2 進厳密値)。**敵対的レビュー4観点 workflow（決定論/忠実度/ジオメトリ/規約・各指摘を独立スケプティック検証）= 検証 12 件 → 確定 0/partial 5/棄却 7 ＝ 実 must-fix 0**。採用 hardening（コメントのみ）: `arc=i*pelletGap` 一様間隔不変条件＋末尾デッドゾーン明記・`corridorLapPhase` の `truncatingRemainder`(truncate-toward-zero)ガード明記。棄却: 末尾デッドゾーンを「破綻」とする誤検出・y-flip/score 位置のズレ誤検出・runtime assert / resampler 改造（sill は doc-contract 流儀）。検証 = `swift build` 緑＋オフスクリーン PNG(食べ/赤フラッシュ/+500/正立を実証)。**Ph5 ライブ確認 PASSED で #12 全フェーズ完了→ここで初めて一括 squash-merge＋最終タグ1本**（未使用版を再確認＝[[parallel-work-hazard]]）。
  - **✅ 済＝Ph3 第一片: directional-eye 直立ゴースト = ライブ確認済**（prism・ユーザー承認 2026-06-22「今の状態でOK」・上の Ph3 行に詳細）。4方向の目はユーザーと prism でライブ微調整して確定（視線方向に目を寄せる＝上下は鏡像で瞳が白の上端/下端・左右は外端で縦中央・**頭の赤シルエットは全方向同一で丸み維持**）。最終グリッドは `Sprite.swift` の `lookRows(_:)`。元 GIF は ~/Desktop/a で transient（プランに ASCII 焼込み済）。
  - **✅ 完了＝Ph3 本体 = PathPet**（任意ジェスチャー線を歩く・head が弧長先行→顔が faceLag 追従・不一致幽霊＝`dampedSine` プルプル）＝初の「動く」カード。ユーザー prism ライブ承認（WIP「Good」→ hardening 後の最終ビルド「OK」）2026-06-22。**直立ゴーストの足場 = `GhostLook.facing(dx:dy:)`・`CanonicalSprite.ghostFrames(look:)` を PathPet で再利用。⚠ #12 マージ保留により未マージ・未 push（`feat-chomp-12` 保持）。**
    - **実装（commit `03f361c` で確保 → 敵対的レビュー後に hardening）**: `Effects.drawChompPath`（pac は tangent で rotate＋口パク・ghost は直立＋`GhostLook.facing`＋`dampedSine`(6/7) パニック・`pathPetPanicHz`=1.5・`showGuide` で角丸トレイル）／純粋 `Trail.markAtArcLength`＋`polylineLength`＋`pathPetCursors`（loop/lag 算を純粋関数化＝CI テスト可・負 now は frameStep 同様 floored で前進）／prism `PathPetView` ライブカード（valid/invalid 2 種）。
    - **敵対的レビュー（4観点 workflow＝ジオメトリ座標/決定論クロック/テスト規約/統合退行・各指摘を独立スケプティック検証）= must-fix 0・nit 14・棄却 0。** 採用 hardening: `drawChompPath` スモークテスト＋`markAtArcLength` 端ケース（過去末端 tangent・ゼロ長セグメント）＋`polylineLength`/`pathPetCursors` 純粋テスト＋負 now パニック fold。**棄却**: 周回 stall の modulo 化（検証で「開パスでは follower を終端へ飛ばす退行」と判明＝現状維持が正）・doc polish。`swift build` 緑。
    - **✅ Ph4 完了**（ネオンコリドー：2 ストローク壁＋内角フィレット＋中央ペレット列＋`positionHash01` で cherry/icon 分け＋`ScaleTier`）＝上の Ph4 行＋引き継ぎ節に詳細。再利用済: `resampleAlongPolyline`/`roundedCornerPath`(#9c)・`polylineLength`/`pathPetCursors`/`markAtArcLength`(Ph3)。**次＝Ph5**（引き継ぎ節参照）。
  - **Ph2 で足場済（再利用）**: `Motion.frameStep`・`PixelArt.chompMouthFrames/Hz`・`CanonicalSprite.waddleFrames/Hz`／canonical 大きい目ゴースト（`Sprite.swift`）・`drawPacMan`/`drawPixelSprite`・prism のライブ NSView パターン（内蔵 60Hz timer＋`PRISM_CHOMP_T` freeze）・`drawLinePets` のピクセル線ペット（y フリップ済）。検証は `swift build`（ローカル）＋ offscreen PNG レンダ（向き確定の Ph1/Ph2 実証手法）＋ ユーザー prism ライブ＋ CI `swift test`。

## アイコンを全面 SVG 化（Phosphor）— いま最優先（1〜2）

> **方針（2026-06-19 決定・ユーザー）**: icon が必要な箇所は**全て SVG**。主役 **Phosphor**(MIT)＋ロゴは **Simple Icons**(CC0)。**後方互換不要・破壊的変更OK**。タイミング一任。見た目と使いやすさ優先・形式は問わない。実装方式はリコンで de-risk 済（下記）。

1. **sill v1.8.0 = SVG アイコン基盤**（= wand 移植の解除点）。 **✅ 完了（PR #52 merged + `v1.8.0` tagged・2026-06-19）**（実装・全要素ライブ撮影確認済・敵対的レビュー反映済。次は #2 の sweep）。
   - ⚠**実装メモ（PR #52）**: SwiftDraw は最新 0.27.0 ではなく **`< 0.25.0`（=0.24.0）にピン** — 0.25.0 が追加した `SVGView.swift` の `#Preview` マクロ（`PreviewsMacros` プラグイン）が **CLT でビルド不可**。0.24.0 は同一 API（`SVG(fileURL:)`/`rasterize`/`NSImage(_:)`）で CLT クリーン。tint 共有関数は `tintedBitmap(base:size:color:scale:)` 名で実装（`pt` は CGSize＝SF を byte-identical に保つため）。`leadingSymbol` の SF→Phosphor リゾルバ差し替えは #2 に分離（基盤では SF 維持＝回帰ゼロ、SVG 入口は `leadingImage`）。prism に `family` 設定キー追加（タブ決定論撮影）。`Package.resolved` は `.gitignore`（lib 慣習）。
   - **レンダラ = SwiftDraw**（github.com/swhitty/SwiftDraw・Zlib・依存ゼロ・純 Swift CoreGraphics・macOS10.15+）を **ThemeKit 専用の package 依存**に追加。✗NSImage 直 SVG（macOS13 で nil＝私的 `_NSSVGImageRep`）／✗アセットカタログ（Xcode/actool 必須＝CLT-only の本機で不可）。**SwiftDraw が CLT でも確定動作する唯一手**。
   - **アイコン源 = phosphor-icons/core**(MIT) の SVG を**使う分だけ** `Sources/ThemeKit/Resources/Phosphor/<weight>/<name>-<weight>.svg` に vendor（regular は無サフィックス・他weightは `-bold` 等）＋ `LICENSE` 同梱。viewBox **256**・`fill=currentColor`＝黒マスク。ロゴは simple-icons(CC0・viewBox24・黒マスク) を `Resources/SimpleIcons/` に subset vendor。
   - **tint は既存経路を流用**: `ThemedFAB`/`ThemedButton` の `tintedSymbol` 下半分（device-pixel bitmap＋`sourceIn`）を共有 `tint(base:pt:color:scale:)` に factor し、**SF も Phosphor も tint 1 本**に。viewBox が大きいので必ず **targetPt×backingScale** でラスタライズ。`(name,weight,pt,color,scale)` でキャッシュ。
   - **ローダ**: `phosphorImage(name:pt:weight:)->NSImage`（@MainActor・Bundle.module→`SVG(fileURL:).rasterize(with:scale:)`）。任意で vendored 名の enum を生成すると refactor-safe。
   - **API churn 最小**: 各ウィジェットの `leadingSymbol: String?` 等は**維持**し、内部リゾルバを SF→Phosphor へ差し替えるだけ（型変更ゼロ）。加えて wand 用に **`leadingImage: NSImage?`**（ThemedButton/FAB）と **`ThemedToolBar.ButtonItem.image`** を追加＝事前解決画像（アプリアイコン/favicon/絵文字/ブランドロゴ・**format 非依存**）。`.isTemplate` で tint・多色は素描画。
   - 必須: prism showcase で Phosphor 数ウェイト＋ロゴを全テーマ表示し**ライブ撮影**で確認（`swift test` は CLT 不可）。⚠タグ前に **v1.8.0 未使用を要確認**（並行セッション）。

2. **全 SF Symbol → Phosphor sweep** — **✅ 完了（PR #53 merged + `v1.9.0` tagged・2026-06-19）**（Sources/ + Tests/ から SF 全廃・prism 全 showcase + `phosphorIcon` ヘルパ・Phosphor regular 31個 vendor・CI `swift test` 緑。→ wand 移植解除）。棚卸し済だった = SF **約45種・呼出約90・16ファイル**。
   - **ウィジェット(5＝load-bearing)**: ThemedComboBox(`chevron.down`→`caret-down`／`xmark.circle.fill`→`x-circle`(fill))・ThemedMenu(`checkmark`→`check`(bold))・ThemedList(行/節 `chevron.right/down`→`caret-right/down`・`sfImage` ヘルパ)・ThemedButton(`tintedSymbol`)・ThemedFAB・ThemedTextField(`drawSymbol`)。ThemedButtonGroup/ThemedToolBar は ThemedButton 経由で自動。
   - **showcase(8)**: 文字列＋ローカル resolver(`menuGlyph`/`glyph`/`favicon`)。Specimens/Gallery の SwiftUI `Image(systemName:)` は `Image(nsImage: phosphorImage(...))` へ。KitCatalog は文言のみ。
   - **主要マップ例**: plus→plus・magnifyingglass→magnifying-glass・line.3.horizontal→list・arrow.clockwise→arrow-clockwise・square.and.pencil→note-pencil・square.and.arrow.up→export・ellipsis→dots-three・gearshape.fill→gear(fill)・paintpalette.fill→palette(fill)・tag→tag・folder→folder・trash→trash・text.alignleft/center→text-align-left/center・bold→text-b(bold)・italic→text-italic・underline→text-underline・list.bullet→list-bullets・1-4.circle→number-circle-one..four（全マップは PR で）。
   - **等価なし＝prism モック限定のみ**: `swift`→file-code・`safari`→compass・`moon.zzz`→moon・`xmark.bin`→trash。**ThemedCheckbox の ✓ は CGPath（SF でない）＝据え置き**。
   - ↓ ここまで揃うと **wand 移植が解除**（以降は wand リポで管理）:
     - **wand PR2a** = sill ピン `0.11.0`→`1.x`。wand が使う sill シンボル(Palette/Effects/ConfigSchema/CLIKit)は 0.11.0↔現行で **API バイト一致**（`git show` 逐一 diff 済）＝実質ノーオペ。1行＋`swift package update sill`＋ビルド確認。
     - **wand PR2b** = WandAdapterMacOS に PaletteKit/ThemeKit 追加＋`wandResolvedPalette(name)=resolve(paletteFor(name))` ブリッジ（list 行は当面 4-slot トークン併存）。`LauncherPanel` の横2経路（`buildContent` の `.toolbar`/`.labeledToolbar`＋`installToolbarLayout`/`installLabeledToolbarLayout`＋ItemRow idle/hover/tracking）を 1 つの `ThemedToolBar`(variant`.compact`/`.dense`・corners`.rounded`・surface transparent/alpha・`trackingMode .nonActivatingPanel`)へ置換。`onItemClick`/`onItemHover`/`frameOnScreen(ofItem:)` 結線。アイコンは `ButtonItem.image`(アプリ/favicon=Simple Icons or 実アイコン)。**据え置き**=縦`.list`全部(子は常に list)・子パネル統括・IconResolver・装飾(rainbow/line-pet/blur/影/開閉アニメ)・neon/splatoon/rainbow(動的入力)・✓/−グリフ・NonActivatingPanel。

## sill に部品を足す — perch 用（3〜7）

3. **`ThemedChip` を新規 — ✅ 完了（PR #54 merged + `v1.10.0` tagged 2026-06-20）。** facet のタグ・ステータス pill・キー記号（⌘⇧）を **1部品**で。perch のヒント pill は当初想定に挙がったが、調査で **オーバーレイは CG ループ描画（`OverlayCanvas.PillLayout`／N個を bespoke アニメ）＝ NSView 1チップでは代替不可**と判明し**スコープ外**に確定（必要なら将来 Spec＋描画ヘルパで別途）。
   - **設計（グリル Q1〜Q9 確定）**: 形＝普通の NSView 部品（ThemeKit 全部品と同骨格・`palette{didSet→applyTheme}`）。タイブレーカー＝**「MUI Chip のセマンティクス＋既存 ThemeKit 部品の慣習」**。
   - **enum**: `Variant{filled, outlined, keycap}`／`Size{small(24h), medium(32h)}`（MUI Chip に large 無し）／`Role{neutral, primary, secondary, error}`（既定 neutral＝MUI `color="default"`。ThemedButton には無い `neutral` を MUI 根拠で追加）。
   - **API**: `title`（uppercase しない）・`leadingSymbol`/`leadingImage`(image優先・trailing 汎用アイコンは無し)・`isSelected`・`onTap?`(非nil⇒クリック可＝hover/press/focus ring/Space)・`onDelete?`(非nil⇒末尾 `x-circle`・Backspace/Delete でも発火)・`preview{Hovered,Pressed,Focused}`。`keyEquivalent`/group seam 系は省略。`isInteractive`(clickable∨deletable)でフォーカス可否を判定（delete-only でも × にキーが届く）。
   - **テーマ契約（正準ロールのみ）**: filled→ role不透明＋`onPrimary`/`onSecondary`/`bestContrast(error)`、neutral=`ink(.wash,of:.muted)`＋`foreground`。outlined→ clear＋role@0.5→hover full（neutral=`border`）。keycap→`ink(.faint,of:.foreground)`＋`foreground`＋`border`＋**mono**（role 無視）。state は ThemedButton 同型（クリック可時のみ）。`isSelected`→`selection`(neutral)/role wash。focus ring=`primary`。
   - **寸法**: 高さ 24/32・font 13・**chip=pill(高さ/2)・keycap=5pt 角＋minWidth=高さ(単グリフ正方)**・chip minWidth 0(中身密着)・leading/×=14/16・gap 5。
   - **prism**: `ChipShowcase.swift`（`ThemedChipView` 橋＋`MockChip`：LIVE/強制状態/role/size/variant＋**keycap 実グリフ `⌘ ⇧ ⌥ ⌘N ⇧⌘N Esc`**）＋Gallery `.action` 結線＋KitCatalog エントリ。
   - **検証**: `swift build` 緑・3テーマ（terminal/github-light/blacklight）ライブ撮影で filled/outlined/keycap/×/isSelected/role(黒白コントラスト ink)/全 state 確認・`Tests/ThemeKitTests/ThemedChipTests.swift`(29 ケース・CI で実行)。
4. **perch の自前エフェクトを sill の既存 Effects に寄せる — ✅ 完了（perch PR #132 merged 2026-06-20・sill 変更なし）。** ネオン枠を perch 自前 hue テーブル→sill `Effects`（`borderEffectFor`/`resolveBorder`/`blendThrough`）に収束。決定 = sill をそのまま採用（facet と同じ list-blend・破壊的変更 OK）。詳細は上の 🧭現在地 #4。
5. **perch の自前の色計算を sill の PaletteKit に寄せる — ✅ 完了（perch PR #133 merged 2026-06-20）**（#4 同型・sill 無改変・byte-identical・3 perch-ism の裁定は上の🧭参照）。
6. アニメ用の計算 `ThemedTransition` を新規 — perch・facet・wand 共通 — **✅ 完了（PR #55 merged + `v1.11.0` tagged 2026-06-20）**（新規 pure モジュール `Motion`＝Duration/Easing(cubic-bezier solver)/Tween/lerp/spring/dampedSine/autoDuration・Effects=巡回 と住み分け・prism「Motion」タブ・CI 緑。詳細は上の 🧭 #6）。
7. 紙吹雪 / 花火を Effects に足す — **✅ 完了（PR #56 merged + `v1.12.0` tagged 2026-06-20）。** 新規 `ParticleBurst` atom（pure closed-form パーティクル：`rollBurst`→`resolveParticles`→AppKit `drawParticles`）＝border flash（`FlashState`/`rollFlash`/`resolveBorder`/`drawLinePets`）と同型の「ロール→wall-clock 減衰」。Euler 積分なし・`f(now)`・Sendable（pure 層は UInt32/Double、CoreGraphics/AppKit は gate 内）。emission `.fireworks`(放射・グロー)/`.confetti`(ポッパー・回転紙片)・`EffectIntensity` で count/reach スケール（hard-cap 6…40）。perch `ParticleDriver` を一般化（wand burst も将来吸収・**アプリ移植は follow-up**＝build-best-then-migrate・本 PR は sill のみ additive）。prism「Particles」タブで両エフェクト live。検証: `swift build` 緑＋prism ライブ撮影（synthwave）＋敵対的レビュー（4観点×独立検証）＋**CI 緑（full Xcode `swift test`／build／lint）→ squash-merge 済**。⚠教訓: 初回 CI が**テスト専用 compile エラー**を捕捉＝`rollBurst(from: [])` 空配列リテラルがタプル/CGPoint 2オーバーロードに ambiguous（`swift build` は test を compile しない＝CI だけが守る）。メモリ [[sweep-include-tests]]。

## sill のその他（8〜11）

> **実行順 = ~~#9~~ ✅ → #11+#10（一括）→ #8**（ユーザー裁定 2026-06-20「#8〜11 は順番入れ替え可」＋ 2026-06-21「#11・#10 は軽量なので一括」）。番号は安定参照として据え置き、着手はこの順。**#9 完了（9a–9d 済・9e 不要）・#11+#10 ✅ 完了（PR #61・prism のみ＝タグ無し）・#8 ✅ 完了（PR #63 + `v1.18.0`・2026-06-22）= 全 #1–#12 完了。**

8. **文字を MUI 規約に照合して調整** — **✅ 完了（PR #63 + `v1.18.0`・2026-06-22）。**
   実装 = **固定内部 TypeScale**（`Palette.TypeRole`→`TypeToken(pt,weight)`／MUI `createTypography.js` を **role で** grounding・macOS native pt に対応／pure・Sendable・CoreGraphics-free）＋ `ResolvedPalette.uiFont(role)`（PaletteKit・**単一** font factory）。全 10 ThemeKit ウィジェットを集約＝各自の `themedFont` 全廃。
   **🔑 実バグ修正**: 旧ヘルパは `.mono`/既定しか分岐せず `.rounded`(6テーマ=rainbow/acidwave/outrun/solar-veil/arcane/dusk)/`.menu` を取りこぼし**素 system 描画**していたのを構造的に解消。
   **可読性修正**（MUI の「小サイズは weight で強調」＝12pt 化より忠実・macOS native 維持）: ThemedList 2 行目＋TextField 補助文 → **11pt medium**（mono URL 行も同一トークン経由）・compact badge **9→10pt**（labelFontSize 下限）。shortcut 10/medium は据え置き（native keycap 階＝変更不要と確定）。`floatSize` は float ラベル比率/notch 専用に分離（補助文と切り離し）。
   GQ#9 を「**FontKind=唯一の themed 軸**／size・weight=固定内部スケール（非 themable）」に進化（DESIGN.md §5 grounding 表 = 原典 grounding の実体）。prism `.palette` タブに `TypeScaleSpecimen`（全 role を実解決フォントで全テーマ live）。
   検証 = `swift build`＋**CI 緑**（full-Xcode `swift test`＝pure `TypeScaleTests`・PaletteKit 解決 fidelity・ThemeKit probe[2行目 weight incl mono／badge=10／補助文 medium／float-scale 不変]）→ squash-merge。**ライブ目視はユーザー**（実装時 2nd セッションが prism 使用中につき委譲＝`.palette` で scale・`.rounded` テーマで family 修正を確認）。
9. **wand のエフェクト一式を sill に移植** — **✅ 完了（スコープ=full・ユーザー裁定 2026-06-21／sill 側は 9a–9d で完結・9e=wand 本体移植は不要）。** 調査(workflow)で4バケットに判明: ✅ line-pet/chomp pet は**既に共有済**（wand は既に `drawLinePets` を呼ぶ）／🟢 burst＋カード confetti/fireworks は **#7 `ParticleBurst` と同形**（rule-of-three 達成 perch＋wand）／🟡 trail ジオメトリ・gesture 認識は**新規**（今は wand のみ＝前方投資）／🔴 `ChompRenderer`(~1000行アーケード署名) は wand 据え置き。**連作 PR（build-best-then-migrate・sill 側を先に・各 minor bump）:**
   - **9a** `ParticleBurst` 拡張 = per-particle `radiusSpeed`(冷却縮小・CAEmitter `scaleSpeed` 相当)。**✅ 完了（PR #57 + `v1.13.0`・2026-06-21）。** 既定0＝#7 とバイト一致・additive。prism 花火に -2.0 実演。（`spinRange` は不要と判断＝丸い spark は spin 不可視・紙片は既に per-particle spin 保有。）
   - **9b** `SplatterShape` 新規 = ink splatter（`DecalManager` 由来）。**✅ 完了（PR #58 + `v1.14.0`・2026-06-21）。** pure（SplitMix64＋tendril-blob 頂点ロール・Sendable・(x:y:) タプル）＋AppKit `drawInkSplatter`（Catmull-Rom）＋prism「ink splatter」section＋seed 決定論。敵対的レビュー2観点クリーン。
   - **9c** trail ジオメトリ primitives = `resampleAlongPolyline`(弧長リサンプラ＝wand `walkPath`)＋`roundedCornerPath`(角丸＝`buildHybridPath`)＋AppKit `nsBezierPath([PathStep])`。**✅ 完了（PR #59 + `v1.15.0`・2026-06-21）。** pure（`Double`/(x:y:)・`TrailMark`/`PathStep`）・終点重複は解消（wand のクセ修正）・prism「trail geometry」section（角丸＋接線シェブロン＋live 走査）。色サイクル/フェードの wand→sill 寄せは wand 本体移植（旧 9e）の領分＝**9e 不要（2026-06-21）に伴い sill ROADMAP では追わない**（必要になれば wand リポ側で実施）。⚠教訓: テストの空配列 `[]` が tuple/CGPoint 2オーバーロードで ambiguous（型注釈で解消・[[sweep-include-tests]]）。
   - **9d** 新規 pure モジュール `Gesture` = `Direction`(L/U/R/D)・`Sample`・`recognize(samples:minStrokePx:)`・`reversals`/`isOpposite`/`patternIssue`・`GestureRecognitionSpec`。**✅ 完了（PR #60 + `v1.16.0`・2026-06-21）。** CLIKit 型 mechanism-only（`Rule`/`Action`/`Target` は wand 据え置き）。4-way=wand パリティ（8-way は後日）。`Sample` は `Double` コア＋`#if canImport(CoreGraphics)` で `CGPoint`/`TimeInterval` 便宜層。logic-only＝XCTest のみ・prism カードなし。wand 元実装と挙動同一（敵対的レビュー fidelity/purity/tests=0 件・idiom 7 件は合格確認）。⚠教訓: `Sample.swift` の `TimeInterval` が `import Foundation` 漏れで `swift build` 一度赤→冒頭に追加で解消（[[sweep-include-tests]] と同じ「CLT ローカルは build のみ・型/import 漏れに注意」系）。
   - **9e** wand 本体移植 = **不要（ユーザー裁定 2026-06-21）。** 元々 wand repo 側の follow-up（burst→`ParticleBurst`・decal→`SplatterShape`・trail→9c＋`blendThrough`・recognition→`Gesture`）で sill ROADMAP では追わない方針。必要になれば wand リポ側で実施。
   - ⚠ 9b/9c/9d は**今は wand のみ消費**（rule-of-three 未達）だが、#7 同様の前方投資としてユーザー承認済（2026-06-21）。build-best＝一般形で作る。
10. prism のタブを app 別に分割 — **✅ 完了（PR #61・2026-06-21・prism のみ＝バージョンタグ無し）。** 裁定=**Option A**（Kit ウィジェットタブ維持＋単一 Chrome タブを app 別 **facet/wand/perch/halo/glance** に分割・各タブ=その app の chrome モック＋実消費 caption・全テーマ grid 維持）。**🔑 発見**: 各 app は ThemeKit ウィジェットをほぼ未使用（facet が `ThemedScroller` 1個のみ・他0）＝実体は「テーマ＋エフェクトで彩色した独自 chrome」（Kit=ライブラリ showcase／App=consumer 実態）。**halo 追加**（表記4→5・意図的逸脱・ユーザー承認 Option A に含む）＝新規 `MockHalo`（focused-window ring=`ThemedBorderView`）。実装=`KitFamily` を kit/app 2グループ化（`.chrome` 廃止）＋`AppChrome` caption テーブル＋ヘッダ2軸＋カード switch 5 app ケース。検証=`swift build` 緑＋ライブ撮影4枚（halo リング glow／facet 全テーマ MockTree／Effects ON·OFF）＋敵対的レビュー3観点（1件=appCaption の per-frame 再 resolve を修正）。プラン → [`docs/superpowers/plans/2026-06-21-prism-app-tabs.md`](superpowers/plans/2026-06-21-prism-app-tabs.md)。
11. prism のエフェクト切り替えトグルの配置直し — **✅ 完了（PR #61・#10 と一括・2026-06-21）。** `EffectToggle` を family タブ列末尾 → **タイトル行（右上）** へ移設（エフェクト=テーマ軸の制御＝タブ列から分離）。#10 のタブ2グループ化（Kit/Apps・各 FlowLayout で wrap）と一体実装・ライブ撮影で ON/OFF がベンチ全体を駆動するのを確認。

## 完了

- **v1.7.0** 横ツールバー `ThemedToolBar` 新規（MUI `<AppBar>`+`<Toolbar>` 融合）— surface(surface/primary/secondary/transparent)・variant(regular64/dense48/compact40)・flex セクション・型付き items（本物の ThemedButton を COMPOSE／icon-only 角ボタン・label・custom・divider・spaces）・非アクティブパネル hover・子パネルアンカー frameOnScreen。prism showcase＋KitCatalog＋決定論テスト。overflow は follow-up。（PR #50・旧 #1）
- **v1.6.0** ThemedList のチャンク並べ替え — ヘッダ＋配下の行を1かたまりで並べ替え（複数行 dim・節境界フル幅 insertion bar・header grip・chunk ghost）＋キーボード DnD を節境界 aim に共通化（KeyboardDragController）（旧 #1）
- **v1.5.0** ThemedList の横スクロール＋キーボードカーソル（.outline で選択と見分け）＋ゼブラ縞（旧 #4/#5/#6）
- **v1.4.0** ThemedList の階層インデント＋折りたたみ（旧 #2）
- **v1.3.0** ThemedList ドラッグ層（行の drop-onto／並べ替え＋キーボード lift）（旧 #1）
- **v1.2.0** スクロールバーの themed 化 + アイコン上下逆の修正 + prism の文字拡大
- **v1.1.0** メニューのサブメニュー（1 段）
