// MarkdownKit — unit tests. These run in CI ONLY (the maintainer's machine is
// CommandLineTools-only; `import XCTest` needs full Xcode).

import XCTest
@testable import MarkdownKit

final class MarkdownKitTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder — real tests added in later tasks.
        XCTAssertTrue(true)
    }
}
